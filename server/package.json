{
  "name": "finguard-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prestart": "cd ../python/outputs && if [ ! -f transactions.db ] && [ -f transactions.db.gz ]; then gunzip -k transactions.db.gz; fi",
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "@google/generative-ai": "^0.21.0"
  }
}
